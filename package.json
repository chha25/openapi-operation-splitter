{
  "name": "openapi-operation-splitter",
  "version": "1.0.0",
  "description": "Split open-api file by operations",
  "main": "app.js",
  "scripts": {
    "build": "tsc -w",
    "start:run": "nodemon build/app.js --sourcePath=a --targetPath=b",
    "test": "jest --coverage"
  },
  "author": "Christoph Hammerbacher",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "jest": "^27.3.1",
    "nodemon": "^2.0.14",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "ts-command-line-args": "^2.2.0"
  }
}
